  <% if cart_item != @current_item %>
  <tr>
  <% else %>
  <tr id="current_item">
  <% end %>
     <td><%= cart_item.quantity %> &times; <%=h cart_item.title %></td>
     
     <td class="cart_item-price"><%= number_to_currency(cart_item.price) %></td>
  </tr>

